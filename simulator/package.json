{
  "name": "meter-simulator",
  "version": "1.0.0",
  "description": "Generates synthetic meter events and publishes them to Redis for the dashboard to consume in real time.",
  "private": true,
  "main": "simulator.js",
  "scripts": {
    "start": "node simulator.js"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "redis": "^4.6.13"
  }
}